# 📝 Handoff: 2026-01-19_22-05
**Zaman:** 19 Ocak 2026, 22:05
**Odak:** Bul/Değiştir, Kaydetme Sistemi ve Bildirimler

## ✅ Tamamlanan İşler
1. **Bul ve Değiştir (Find & Replace):**
   - `useCsv` hook'una `findAndReplace` fonksiyonu eklendi. Büyük/küçük harf duyarlılığı ve sütun bazlı hedefleme desteği sunuyor.
   - `FindReplaceDialog` bileşeni oluşturuldu ve toolbar'a entegre edildi.

2. **Kaydetme ve Dışa Aktarma:**
   - `saveCsvFile` fonksiyonu ile mevcut dosyayı güncelleme veya `save` diyaloğu ile "Farklı Kaydet" özelliği eklendi.
   - `Papa.unparse` ile JSON -> CSV dönüşümü ve `writeTextFile` ile disk yazma sağlandı.

3. **Bildirim Sistemi (Sonner):**
   - Uygulama genelinde `sonner` toast bildirimleri aktif edildi.
   - Dosya yükleme, kaydetme ve bul/değiştir işlemleri için kullanıcı geri bildirimi sağlandı.

4. **Tauri İzinleri:**
   - `capabilities/default.json` güncellenerek `fs:allow-write-text-file` ve ilgili dosya sistemi izinleri tanımlandı.

## 📁 Değişen Dosyalar
- `src/hooks/use-csv.ts`: Kaydetme ve Bul/Değiştir mantığı.
- `src/lib/file-utils.ts`: Dosya yazma yardımcı fonksiyonu.
- `src/App.tsx`: Toolbar güncellemeleri ve diyalog entegrasyonu.
- `src/main.tsx`: Sonner Toaster eklemesi.
- `src/components/find-replace-dialog.tsx`: Yeni diyalog bileşeni.
- `src-tauri/capabilities/default.json`: FS yazma izinleri.

## 📌 Nerede Kaldık? (Sıradaki Adımlar)
- **Undo/Redo (Geri Al):** Hücre bazlı değişiklik geçmişi tutma.
- **Gelişmiş Filtreleme:** Sütun bazlı metin filtreleme kutuları.
- **Veri Tipleri:** Sayısal ve tarihsel veriler için özel sıralama ve validasyon.

## 💡 Teknik Notlar
- `saveCsvFile` içerisinde `toast.loading` kullanılarak asenkron işlem takibi yapılıyor.
- FS izinleri için uygulamanın yeniden başlatılması (`npm run tauri dev`) gereklidir.
