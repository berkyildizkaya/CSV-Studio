# 📝 Handoff: 2026-01-20_02-55
**Zaman:** 20 Ocak 2026, 02:55
**Odak:** Global Uyumluluk, Güvenlik İzinleri ve Locale Düzeltmeleri

## ✅ Tamamlanan İşler
1. **Global Uyumluluk (BOM Desteği):**
   - Kaydetme işlemlerine **UTF-8 BOM (Byte Order Mark)** desteği eklendi.
   - Excel ve diğer kurumsal araçların karakterleri/sayıları hatasız tanıması sağlandı.
   - `SaveConfigDialog` üzerinden BOM özelliğini açıp kapatma imkanı getirildi (varsayılan: açık).

2. **Güvenlik ve Dosya İzinleri:**
   - Tauri v2 `fs:allow-write-file` izni `capabilities/default.json` dosyasına eklendi.
   - BOM yazımı için kullanılan binary yazma işleminin yetki hatası vermesi engellendi.

3. **Locale (Dil) Düzeltmeleri:**
   - `en.json`, `de.json` ve `tr.json` dosyalarındaki JSON syntax hataları (illegal tab characters) giderildi.
   - Tüm kontrol karakterleri standartlara uygun şekilde `\t` -> `\\t` olarak escape edildi.

4. **Sütun Yönetimi Final:**
   - Sütunların arasına veri ekleme özelliği (`insert at index`) tamamlandı.
   - Sütun taşıma butonları daha interaktif hale getirildi (pointer imleç ve hover efektleri).

## 📁 Değişen Dosyalar
- `src-tauri/capabilities/default.json`: Yeni dosya yazma izinleri.
- `src/lib/file-utils.ts`: Binary yazma ve BOM yönetimi.
- `src/hooks/use-csv.ts`: BOM seçeneği entegrasyonu ve CRUD geliştirmeleri.
- `src/components/save-config-dialog.tsx`: BOM Checkbox eklemesi.
- `src/i18n/locales/*.json`: JSON syntax düzeltmeleri ve yeni anahtarlar.

## 📌 Nerede Kaldık? (Sıradaki Adımlar)
- **Veri Temizliği:** Sütun bazlı toplu düzenleme araçları.
- **Undo/Redo:** Genel geri alma sistemi.

## 💡 Teknik Notlar
- `writeFile` API'si binary (Uint8Array) veri beklediği için metinler önce `TextEncoder` ile encode edilip BOM byte'ları ile birleştiriliyor.
- JSON dosyalarındaki görünmez "literal tab" karakterleri Vite derleyicisinin çökmesine neden oluyordu, bu karakterler temizlenerek stabilite sağlandı.
